# 다음 세션에서 할 일 (2026-08-17 갱신)

**0.6.4 릴리스.** 업데이트 확인이 뒤처진 사이트에 발이 묶이던 것을 고쳤다.

⚠ **하네스는 0.6.4 에서 아직 돌리지 않았다.** `tsc` 와 `vite build` 만 통과시켰다
(0 errors). 0.6.3 기준으로 **173개 통과 / 0 실패 / SKIP 2** 였고 이번에 검사를 하나
더했으니, `VITE_ICECMD_HARNESS=1 npm run tauri dev` 로 174 를 한 번 확인할 것.
릴리스 직후에 돌리면 `site version is not behind what shipped` 가 잠깐 붉을 수 있다 —
CI 가 릴리스를 붙이고 `latest.json` 을 올리기 전까지의 창이다.

릴리스만으로 끝나지 않는다는 것을 0.6.3 이 증명했다 — 아래 **릴리스 절차** 를 볼 것.

---

## 0.6.4 에 들어간 것

### 사이트가 뒤처지면 업데이트가 조용히 멈췄다 (0.6.3 을 배포한 뒤 발견)

태그도 릴리스도 정상이었는데 **아무에게도 새 버전이 뜨지 않았다.** `src/update.ts` 는 사이트의
`latest.json` 을 먼저 보고, 그 파일이 **읽히기만 하면 그 답을 최종으로** 삼았다(`if (info) return
null`). 사이트 갱신을 잊어 `0.6.2` 라고 답하는 동안 GitHub 릴리스 API 는 조회조차 되지 않았다.
다운로드 카드도 같은 파일을 읽으니 카드까지 0.6.2 를 주고 있었다.

→ 읽혔다는 것만으로 끝내지 않는다. 사이트가 **뒤처져 있어도** GitHub 을 본다. 대가는 둘 —
최신일 때는 두 곳을 다 보므로 켤 때 요청이 하나 늘고, 사이트를 잊은 동안에는 앱이 카드보다
새 버전을 안내한다. 그때 링크는 GitHub 자산이라 설치 파일은 제대로 받는다.

하네스도 함께 고쳤다. 기대값을 `siteVersion` 하나로 잡고 있어서 **이 사고를 정답으로 판정했다.**
이제 두 출처를 다 훑어 "먼저 새 버전을 답하는 쪽"을 기대하고,
`a stale site does not hide a newer github release` 를 따로 둔다. (`site version is not behind
what shipped` 는 원래 있었다 — 릴리스 뒤에 하네스를 한 번 돌렸다면 이것만으로도 붉게 떴다.)

**0.6.3 을 쓰는 사람에게는 이 고침이 닿지 않는다.** 그들의 앱에는 옛 코드가 들어 있다. 다만
사이트가 최신이므로 0.6.4 는 정상적으로 안내된다 — 이 고침이 값을 하는 것은 *다음번에* 사이트
갱신을 잊었을 때다.

---

## 0.6.3 에 들어간 것

### 프로젝트를 떠나면 두고 온 셸이 13칸으로 줄어 있었다

숨은 스테이지는 `display: none` 이고, 그 안에서 계산된 스타일에 폭을 물으면 `.terminal-host` 의
`100%` 가 해석되지 않은 채 문자열 그대로 돌아온다. FitAddon 은 그것을 100 **픽셀** 로 읽는다.
떠나는 순간 페인의 ResizeObserver 가 울리고, 그 100픽셀짜리 추측이 그대로 PTY 까지 갔다 —
실측 124칸 → 13칸.

셸이라면 다음 줄만 접히고 만다. 그런데 claude·codex 는 통보받은 폭에 맞춰 화면 전체를 다시
그리고, 그것을 **진짜 개행과 박스 문자로** 한다. 그래서 돌아와 격자를 넓혀도 xterm 이 펼 수
있는 것이 남아있지 않다.

→ 화면에 없는 페인은 아예 재지 않는다(`isMeasurable`). `clientWidth`/`clientHeight` 는
레이아웃되지 않은 요소에 대해 0 이고, 그것이 진짜 측정값으로 오해될 수 없는 유일한 답이다.
숨은 채 태어난 페인은 120×30 으로 시작하고, 처음 보이는 순간 `syncSize` 가 실제 크기로 바꾼다.

### 폴더를 열어두고 다른 프로젝트에 다녀오면 닫혀 있었다

열린 행이 오른쪽 패널이 프로젝트마다 remount 하는 컴포넌트의 `useState` 에 있었고, 마운트
효과가 한 번 더 비웠다. → 스토어로 옮겨 프로젝트별로 기억하고 state 파일에 남긴다. 마운트할
때 루트뿐 아니라 **이미 열려 있는 폴더까지 함께 읽는다** — 그러지 않으면 펼쳐진 채 "…" 만 남는다.

---

## 0.6.2 에 들어간 것 — 다른 PC에서 잡힌 셋

### 어떤 PC에서는 claude 가 전부 흑백이었다

색이 **틀리게** 나온 게 아니라 **아예 안 나왔다.** 그건 렌더러 문제가 아니라 CLI 가 색을 안 쓰기로
결정했다는 뜻이고, 그 결정의 근거는 **자식이 물려받은 환경**이다.

`TERM`·`COLORTERM` 은 "여기는 컬러 터미널"이라고 **알려주는** 것이다. 그것만으로 부족한 경우가
둘 있다 — CLI 는 대부분 **자기 stdout 이 터미널인지**를 먼저 보고, 기계에 박혀 있는 `NO_COLOR`
하나가 그 위의 모든 것을 이긴다. 둘 다 우리가 무슨 말을 했는지와 무관하게 작동한다.

→ **claude·codex 페인에는 통보한다**: `FORCE_COLOR=3`(Node 의 `supports-color` 가 TTY 검사보다
**먼저** 읽는다) · `CLICOLOR_FORCE=1` · `NO_COLOR` 제거.
**일반 셸은 기본으로 두었다** — 거기서 색을 강제하면 `dir > list.txt` 같은 리다이렉트 출력에도
이스케이프가 섞인다. 그 기계에서 셸로 CLI 를 직접 띄우는 경우를 위해 설정에 스위치를 뒀다.

하네스는 **설정값이 아니라 자식이 받은 환경**을 본다. 셸 안에서 `echo [%TERM%][%COLORTERM%]
[%FORCE_COLOR%]` 를 되물어, 스위치 끔=강제 안 됨 / 켬=`[3]` 을 확인한다. 스토어가 값을 들고 있는
것은 프로세스가 그 값을 받았다는 증거가 아니다.

### 더블클릭이 두 가지 일을 하고 있었다

폴더 행은 **클릭이면 펼치고 더블클릭이면 탐색기를 띄웠다.** 트리를 빠르게 두 번 내려가면
아무도 부르지 않은 탐색기 창이 떴다. 트리를 훑는 손짓과 창을 띄우는 손짓이 **같은 손짓**이었고,
얼마나 빨리 눌렀는지로만 구분됐다.

→ 폴더를 여는 것은 **우클릭 메뉴**로 옮겼다(원래부터 거기 있었다). 파일은 펼침과 충돌할 게
없으므로 **더블클릭으로 연다.**

### 사용량이 `/usage` 를 쳐야만 움직였다

claude 는 **이유가 있을 때만** `~/.claude.json` 의 사용량 캐시를 갱신한다 — 시작할 때, `/usage`
를 열 때. 그 사이에 한 창을 3분의 1 태워도 파일은 몇 시간 전 숫자 그대로다. 하단 바는 "2시간 전"
이라고 **정직하게** 적고 있었고, 정직하게 쓸모가 없었다.

→ 캐시가 낡았을 때만, CLI 가 읽는 것과 **같은 엔드포인트**(`/api/oauth/usage`)에서 직접 읽는다.
토큰은 claude 가 저장해 둔 것을 읽는다. **창이 보일 때만 · 90초에 한 번 · 약 2KB.**
만료 토큰은 쓰지 않는다(갱신은 CLI 의 일이다). 실패·무토큰·macOS 키체인이면 **조용히 캐시로
돌아간다** — 0.6.1 과 같은 답이지 더 나쁜 답이 아니다. codex 는 세션 로그에 매 턴 남기므로
원래부터 신선하다.

하네스가 이걸 증명한다: `live=…892205 cached=…942465` — 실시간 값이 캐시보다 **15분 앞섰다.**

### 검사 하나가 틀린 질문을 하고 있었다

"설치본의 state.json 이 이번 실행으로 바뀌지 않았다"를 **바이트 수 일치**로 보고 있었다.
그런데 **이 앱은 이 앱 안에서 개발한다** — 하네스가 도는 동안 설치본이 거의 항상 열려 있고,
사용자가 페인을 하나 옮기면 설치본이 자기 상태를 저장한다. 그래서 검사가 실패했는데 그 이유가
**검사 대상과 아무 상관이 없었다.** 이 파일에 적힌 나머지 이야기와 똑같은 함정이다.

→ 막아야 하는 것은 **지워지거나 덮어써지는 것**이다(2026-08-09 에 실제로 일어난 사고).
지워짐은 0 으로, 덮어써짐은 **dev 스냅샷과 같은 크기**로 드러난다. 그 둘을 본다.

---

## 0.6.1 에 들어간 것 — 0.6.0 을 쓰다 대표님이 잡아낸 둘

### 화면이 덜 그려지던 것 — 스크롤하면 되살아나는 그 증상

xterm 은 **바뀌었다고 아는 행만** 그린다. 옳은 최적화지만, **캔버스가 비었는데 xterm 이
그걸 모르는** 경우가 둘 있다:

1. 프로젝트를 오갈 때 안 보이는 페인의 WebGL 을 떼고(`detachWebgl`) 돌아올 때 **새 렌더러를
   붙인다.** 새 캔버스는 백지인데 버퍼의 행들은 "안 바뀌었다".
2. 창이 최소화되거나 가려지면 WebView2 가 GPU 표면을 버린다. 돌아와도 아무도 알려주지 않는다.

그래서 **새 출력이 닿은 아래쪽만 남고 위는 빈다.** 스크롤이 고치는 것처럼 보이는 이유는
스크롤이 결국 모든 행을 dirty 로 만들기 때문이다.

→ `termRegistry.repaint()` 가 전 행을 한 번 다시 그린다. 부르는 자리는 네 곳:
WebGL 을 붙인 직후 · 컨텍스트를 잃고 DOM 렌더러로 내려간 직후 · 프로젝트로 돌아올 때 ·
창 포커스/visibility 가 돌아올 때. **`syncSize` 로는 안 된다** — 격자 크기가 그대로면
아무것도 다시 그리지 않는 게 그 함수의 일이다.

### 종이 안 꺼져서 "실행 중" 스피너가 영영 안 뜨던 것

`attention` 을 끄는 곳이 **사이드바 행 클릭 한 군데**뿐이었다(`Sidebar.select()`).
**이미 보고 있는 프로젝트에는 누를 행이 없다.** 거기서 울린 종은 영원히 안 꺼지고,
종은 busy 보다 순위가 높으므로 **한 번 걸리면 그 세션 내내 스피너가 다시 안 뜬다.**

하네스가 못 본 이유가 교과서적이다: 검사는 종을 **다른** 프로젝트에서 울린 뒤
`clearAttention()` 을 **손으로 불러** 확인했다. 목적지는 검사했고 **거기 닿는 길은 안 봤다.**

→ `status.tick()` 이 매 틱마다 판정한다(`bellIsSeen(활성, 창이 앞)`). 창이 뒤에 있으면
안 끈다 — 작업표시줄은 어느 프로젝트가 불렀는지 말해주지 않으니 그때는 기다려야 한다.

덤으로 `lastOutputAt` 초기값 `0` 때문에 **앱을 켠 직후 2.5초 동안 모든 프로젝트가 스피너를
돌고 있었다**(`performance.now()` 가 작아서 "방금 출력함"으로 읽힌다). `-Infinity` 로 바꿨다.

---

## 0.6.0 에 들어간 것 — 드래그를 되살리고, 설정을 모으고, 폴더가 따라온다

### 창 아래 검은 줄은 우리 색이 아니었다

터미널은 **행 높이의 정수배만** 채운다. 마지막 행 아래에 한 행 미만의 자투리가 남고, 그
자투리를 그리는 것은 xterm 의 뷰포트인데 `@xterm/xterm/css/xterm.css` 가 그 배경을 **`#000`
으로 못박아** 두었다. 테마 배경은 `#1e1e1e` 이므로 그 띠만 순검정으로 떠서, 창 하단을 가로지르는
검은 줄로 보였다. 스크린샷 픽셀로 확인한 값이 정확히 `rgb(0,0,0)` 이었다.
→ `styles.css` 의 `.terminal-host .xterm-viewport` 가 덮는다. **이 규칙을 지우면 다시 나온다.**

### 0.5.0 의 "트리에서 끌어다 놓기"는 실제로는 죽어 있었다

`dragDropEnabled: true` 면 **Windows 에서 웹뷰의 드래그 배관을 Tauri 의 네이티브 파일 드롭이
통째로 가져간다.** `dragstart`·`dragover`·`drop` 이 아예 발생하지 않는다. 0.5.0 은 그것을 HTML5
드래그로 만들었고, 하네스는 `DragEvent` 를 스스로 만들어 던졌기 때문에 **통과했다.**
README 에 "네이티브 파일 드롭이 HTML5 DnD 를 가린다"고 이미 적어 두고도 같은 함정에 빠졌다.

→ `terminal/pathDrag.ts` 를 **포인터 드래그**로 다시 썼다. 임계값 5px, `<body>` 에 붙는 이름표,
`elementFromPoint` 로 페인을 찾고, 놓은 페인이 포커스를 가져간다. 이름표에 `pointer-events: none`
이 없으면 히트테스트가 자기 자신만 계속 찾는다 — 그 한 줄이 핵심이다.
**새 드래그를 만들 때 HTML5 DnD 를 쓰지 마라.** 셋(프로젝트 순서·경로·페인 재배치) 다 포인터다.

### 프로젝트 순서 드래그 — 위험한 건 배열이 아니라 DOM 이다

배열 순서를 바꾸면 React 가 **키가 같은 자식들의 DOM 노드를 옮긴다.** 그 노드 안에 살아 있는
터미널이 들어 있다. 그래서 `App` 은 스테이지를 **사이드바 순서가 아니라 id 순으로** 그린다 —
어차피 한 번에 하나만 보이므로 화면상의 순서는 아무 뜻이 없고, 대신 노드가 절대 움직이지 않는다.
하네스가 재정렬 직후 `%ICEVAR%` 를 되물어 **같은 셸 프로세스인지 확인**한다.

### 우클릭이 두 번 붙여넣던 것

마우스 리포팅을 켠 TUI(claude·codex 둘 다 켠다)에는 **버튼 2 누름도 이스케이프로 전달된다.**
그 CLI 가 우클릭을 붙여넣기로 처리하면, 앱이 메뉴를 띄우는 동안 CLI 는 CLI 대로 붙여넣는다 —
한 번 눌러 두 번 들어간다. → `TerminalPane` 이 **캡처 단계에서** `mousedown`·`mouseup`·`auxclick`
의 버튼 2 를 삼킨다. xterm 의 요소보다 위(호스트)에서 끊으므로 터미널까지 내려가지 않는다.
왼쪽 버튼은 그대로 통과해야 한다(선택·포커스가 거기 달려 있다) — 하네스가 둘 다 확인한다.

### 폴더가 저절로 따라온다

`notify` (Windows 는 `ReadDirectoryChangesW`) 로 **화면에 보이는 폴더만 비재귀**로 지켜본다.
프로젝트 루트를 재귀로 걸면 `node_modules`·`target` 때문에 이벤트가 폭발한다.
버스트는 250ms 조용해질 때까지 모았다가 한 번만 방출하고(최대 1.5초), 프런트는 **바뀐 그 폴더만**
다시 읽는다. 접힌 폴더는 감시하지 않으므로 **펼칠 때 항상 다시 읽는다.**

---

## 하네스에서 배운 것 (누적)

- **검사가 통과한다고 기능이 사는 것은 아니다.** 이벤트를 직접 만들어 던지는 검사는
  **배관이 열려 있는지는 증명하지 못한다.** (HTML5 드래그)
- **목적지만 검사하고 거기 닿는 길을 안 볼 수 있다.** (안 꺼지던 종)
- **설정값을 검사하지 말고 자식이 받은 것을 검사하라.** (FORCE_COLOR)
- **검사가 실패하면 대상보다 먼저 검사 도구를 의심하라.** 터미널의 "행"은 "줄"이 아니어서
  2359 바이트가 235 로 읽혔고, 설치본 state.json 은 설치본이 스스로 저장하고 있었다.
- **포커스 없는 `navigator.clipboard.readText()` 는 거부하지 않는다. 응답하지 않는다.**
  `await` 로 걸어 두면 그 자리에서 멈춘다. `AppActivate` 는 **성공이라고 답하면서 아무것도
  안 바꾼다** — 창을 앞으로 못 끌어낸다. 대신 `document.hasFocus()` 만 갈아 끼우면 우리 로직은
  진짜로 실행된다.

## 사람이 봐야 하는 것 (하네스가 못 본다)

- **폴더 더블클릭에 아무 일도 일어나지 않는지, 파일 더블클릭은 열리는지.** 여는 쪽을 하네스가
  누르면 진짜로 프로그램이 뜨므로 검사에 넣지 않았다. DOM 배선만 확인한다.
- **그 기계(색이 흑백이던 PC)에서 claude 를 띄워 색이 돌아왔는지.** 원인이 환경이 아니라면
  이 수정으로는 안 고쳐진다 — 그때는 그 창에서 `echo %FORCE_COLOR%` 와 `echo %NO_COLOR%` 를
  찍어 보내 주면 다음 갈래를 판단할 수 있다.
- **실제 마우스로** 폴더 트리 행을 끌어 터미널에 놓기. 프로젝트를 끌어 순서 바꾸기.
- **claude/codex 를 띄운 페인에서 우클릭.** 중복 붙여넣기.
- 탐색기에서 빈 가운데로 폴더 끌어다 놓기. 실제 한글 타이핑.

## 다음에 손댈 만한 것

- **macOS 빌드.** `open`·`open -R` 경로는 넣어 뒀지만 한 번도 빌드해 본 적이 없다.
  `latest.json` 의 `mac` 도 `null` 이다. 사용량 실시간 갱신도 macOS 에서는 **동작하지 않는다**
  (토큰이 키체인에 있다) — 캐시로 돌아가므로 깨지지는 않지만, 하려면 `security` 를 읽어야 한다.
- **폴더 트리 헤더/빈 공간 우클릭** — 지금은 행에만 메뉴가 있다.
- **`여기서 cmd 열기` 에 claude/codex 도** 넣을지. 지금은 셸만.
- 사용량 실시간 갱신이 실패했을 때(로그아웃 등) 바에 그 사실을 보여줄지. 지금은 조용히 캐시로
  돌아가고, 표시는 "N시간 전" 으로만 드러난다.

---

## 릴리스 절차에서 걸렸던 것 (그대로 유효)

**`git tag v0.6.0` 는 `git push --follow-tags` 로 올라가지 않는다.** `--follow-tags` 는
annotated 태그만 민다. `git tag -a` 로 만들고 `git ls-remote --tags origin` 으로 확인한다.

**릴리스가 나간 것과 사용자에게 뜨는 것은 다른 일이다.** 0.6.3 은 태그·CI·릴리스가 모두
정상이었는데 사이트 `latest.json` 을 갱신하지 않아 8시간 동안 아무에게도 뜨지 않았다.
이제 코드가 GitHub 으로 떨어지므로 같은 실수로 업데이트가 멈추지는 않지만, 카드는 여전히
이 파일만 읽는다. 릴리스 뒤에 **끝까지** 할 것:

```bash
gh release edit vX.Y.Z --notes-file <파일>   # 본문이 Full Changelog 한 줄이면
                                             # latest.json 의 notes 가 빈 문자열이 된다
python scripts/make-latest-json.py --write
cd "D:/Naver MYBOX/11. Business/icenovel.com/web/.deploy"
python deploy.py plan
python deploy.py push --approved-by "<실제 지시>"
curl -s "https://icenovel.com/download/icecmd/latest.json"   # 눈으로 버전 확인
```

한글 본문은 **`--notes-file` 로 넘긴다.** 명령줄 인자로 박으면 이 기계의 셸에서 깨진다.

## 미결·잔여

- **클립보드 왕복 2건은 SKIP.** 창을 클릭해 포커스를 준 상태로 하네스를 돌리면 실제로 수행된다.
- **`reveal_path` 의 "선택된 상태로 열림"은 사람이 봐야 한다.**
- 0.2.0 에는 업데이트 확인 코드가 없다(`v0.2.0` 태그에 `src/update.ts` 없음).
