# 다음 세션에서 할 일 (2026-08-11 갱신)

**0.5.0 릴리스 완료.** GitHub 릴리스·사이트 `latest.json`·MYBOX 보관본까지 모두 0.5.0.
서비스 페이지(`/service/icecmd`) 본문도 새 기능에 맞춰 갱신했다.

하네스: `VITE_ICECMD_HARNESS=1 npm run tauri dev` → **115개 통과 / 0 실패 / SKIP 1**.

---

## 0.5.0 에 들어간 것 — 한글 입력·드래그·커서·사용량

릴리스는 나갔지만 **대표님 육안 확인이 남은 것 두 가지**가 아래 "사람이 봐야 하는 것"에 있다.

- **한글이 두 번 입력되던 것.** 원인은 xterm.js 안의 **경합**이었다. WebView2 의 한글 IME 는
  조합 이벤트(`compositionstart/end`) 없이 완성된 음절을 던질 때가 있는데, 그때 xterm 은
  `keydown(keyCode 229)` 에서 "textarea 차이를 나중에 비교해 보내는" 예비 경로를
  `setTimeout(0)` 으로 예약한다. 그 타이머가 IME 의 `input` 보다 **뒤에** 터지면 `keypress`
  가 이미 보낸 음절을 한 번 더 보낸다. 앞에 터지면 차이가 0 이라 안 보낸다 — 그래서
  **같은 줄에서 `안` 은 중복되고 `녕` 은 멀쩡했다.** 간헐적으로 보인 이유가 이것이다.
  → `terminal/ime.ts` 가 `keyCode 229` 키다운을 조합 헬퍼에 닿기 전에 끊어 그 예비 경로를
  아예 예약되지 않게 한다. 조합 이벤트가 정상으로 뜨는 IME 에서는 그 경로가 애초에
  호출되지 않으므로 정상 경로는 건드리지 않는다.
- **앱 안에서 시작한 드래그는 Tauri 를 안 거친다.** 탐색기 드롭은 Tauri 가 웹뷰보다 먼저
  가로채지만(그래서 `sidebar/dnd.ts` 가 있다), 폴더 트리 행에서 끄는 건 평범한 HTML5
  드래그다. 둘은 **다른 배관**이라 `terminal/pathDrag.ts` 를 따로 뒀다. 새 드래그 소스를
  만들 때 `dnd.ts` 를 재사용하려 하지 마라 — 이벤트가 오지 않는다.
- **빈 가운데에 폴더를 놓으면 프로젝트로 등록.** 페인이 하나라도 있으면 가운데 타깃은
  `element()` 가 `null` 을 돌려 **스스로 물러난다.** 그래서 페인이 있을 땐 종전대로
  "경로 입력"이고 비었을 때만 "프로젝트 등록"이다. 두 동작이 서로 뺏지 않는 지점이 여기다.
- **커서가 미친 듯이 깜빡이던 것.** xterm 은 **커서가 움직일 때마다 깜빡임 애니메이션을
  재시작**한다. codex/claude 처럼 계속 다시 그리는 TUI 에서는 그게 스트로브가 된다.
  → 출력이 오는 동안 커서를 고정하고 0.7 초 조용해지면 되살린다(`termRegistry.holdCursor`).
- **하단 상태줄에 claude·codex 사용량.** 두 CLI 가 이미 로컬에 답을 적어 둔다:
  `~/.claude.json` 의 `cachedUsageUtilization`, 최신 `~/.codex/sessions/**/rollout-*.jsonl`
  의 마지막 `rate_limits`. **네트워크 호출도 트랜스크립트 스캔도 필요 없다.**
  다만 **둘 다 그 CLI 가 마지막으로 돌았을 때의 캐시**라 값이 멈출 수 있어서, 화면에
  "N분 전"을 같이 찍는다. mtime 이 바뀔 때만 다시 읽는다(`usage.rs` 의 `memoized`).

### 이번에 하네스가 잡아낸 진짜 결함

`cli_usage` 를 **동기 커맨드로 두었더니 첫 페인의 셸이 늦게 떴다.** Tauri 는 동기 커맨드를
메인 스레드에서 돌리는데, 그 스레드가 `create_session` 을 처리하는 곳이다. 거기서
`.claude.json` 파싱과 1MB 세션 로그 읽기를 하고 있었다 — **상태줄 때문에 터미널이 기다리는
구조**였다. `async fn` 으로 바꿔 메인 스레드에서 내렸다.
**교훈: 파일을 읽는 커맨드는 `async` 로 둔다.** 증상은 엉뚱한 곳(셸 프롬프트 지연)에서 났다.

### 릴리스에서 걸린 것

**`git tag v0.5.0` 는 `git push --follow-tags` 로 올라가지 않는다.** `--follow-tags` 는
annotated 태그만 민다. 커밋만 조용히 올라가고 태그는 남아서, 워크플로가 돌지 않는데도
릴리스를 기다리게 된다. `git tag -a v0.5.0 -m "..."` 로 만들고
`git ls-remote --tags origin` 으로 확인한다. README 의 릴리스 절차도 고쳐 뒀다.

### 사람이 봐야 하는 것 (하네스가 못 본다)

- **탐색기에서 빈 가운데로 폴더를 실제로 끌어다 놓기.** Tauri 가 OS 드래그를 웹뷰보다 먼저
  가져가서 DOM 이벤트가 없다 — 기존 파일 드롭 검사와 같은 한계다.
- **실제 한글 타이핑.** 하네스는 "예비 경로가 예약되지 않는다"까지만 증명한다.

---

## 0.4.0 에 들어간 것

- **우클릭 메뉴가 동작한다.** 0.1.0~0.3.0 내내 죽어 있었다. 메뉴를 닫는 리스너가 메뉴 자신을
  누르는 press 까지 바깥으로 봐서, 누르는 순간 버튼이 언마운트되고 `click` 이 성립하지 않았다.
- **`chrome/ContextMenu.tsx` 하나로 통일.** 사이드바·폴더 트리·터미널이 같이 쓴다.
  메뉴를 손으로 또 만들면 위 버그가 그대로 복제되므로, 새 메뉴는 반드시 이 컴포넌트로 만든다.
- **폴더 트리 우클릭** — 파일: `열기`·`폴더에서 보기` / 폴더: `탐색기에서 열기`·`여기서 cmd 열기`.
  Rust `open_path`·`reveal_path`. explorer 의 `/select` 는 경로에만 따옴표가 붙어야 해서
  `raw_arg` 로 명령줄을 직접 만든다 — 공백 있는 경로에서만 드러나는 차이다.
- **터미널 우클릭** — `복사`(선택이 있을 때)·`붙여넣기`. Ctrl+Shift+C/V 와 같은 구현
  (`terminal/clipboard.ts`).
- **파일·폴더를 터미널에 끌어다 놓으면 경로 입력.** 공백·`&`·`^`·`%` 등이 있으면 따옴표
  (`terminal/dropText.ts`). 실행하지 않고 개행도 보내지 않는다. 드롭 리스너(`sidebar/dnd.ts`)는
  사이드바와 각 페인이 공유하고, 각자 자기 사각형으로 안/밖을 판정한다.
- **Edge 기본 우클릭 메뉴 차단.** Shift+우클릭만 통과(요소 검사용).
- **개발 빌드는 `state.dev.json`** + 저장 시 `.bak` 한 세대. 설치본 설정을 지우던 사고 차단.
- 폴더 헤더 아이콘을 인라인 SVG 로. **LICENSE(MIT)** 추가.

## 다음에 손댈 만한 것

- **macOS 빌드.** `open`·`open -R` 경로는 이미 넣어 뒀지만 한 번도 빌드·실행해 본 적이 없다.
  `latest.json` 의 `mac` 도 `null` 로 비어 있다.
- **폴더 트리 헤더/빈 공간 우클릭** — 지금은 행에만 메뉴가 있다.
- **`여기서 cmd 열기` 에 claude/codex 도** 넣을지. 지금은 셸만.

---

## 하네스에서 배운 것 (다음에 또 당하지 않으려면)

- **검사가 자기 질문을 읽고 있었다.** `askShell` 이 명령줄에 토큰을 박아 보냈는데, 콘솔은
  타이핑을 그대로 에코하므로 셸이 답하기 전에 화면에 `TOKEN=…` 이 이미 있었다. 파일 크기를
  전부 `%~zA)` 로 읽으면서 검사 3개가 **아무것도 측정하지 않은 채 통과**했다. 지금은 토큰을
  셸 변수에 넣어 명령줄에서 빼고, **답의 모양(`^\d+$`)까지 검사**한다.
- **하네스가 hydrate 와 경쟁했다.** 저장 상태 복원이 비동기라, 프로젝트를 먼저 추가하면
  복원분이 그 위에 얹혀 페인 수가 하나씩 밀리고 무관한 검사 7개가 같이 무너졌다
  (원인은 하나인데 실패는 일곱으로 보인다). 지금은 `hydrated` 를 기다린 뒤 **이전 실행이
  남긴 프로젝트를 지우고** 시작한다.
- **경로를 라벨로 재구성하지 마라.** 트리 행이 `data-path`, 페인 슬롯이 `data-pane` 으로
  자기 정체를 들고 있고 검사는 그걸 읽는다. 재구성하던 때 엉뚱한 경로와 비교해 오진이 났다.
- **하네스 실행 중에는 소스를 건드리지 마라.** vite 가 리로드하면 하네스가 다시 돈다.
- **포트 1420 이 남아 있으면 조용히 실패한다.** 이전 실행의 vite 가 살아 있으면
  `Port 1420 is already in use` 로 죽는다. 실행 전에 `icecmd.exe` 와 1420 리스너를 정리한다.

## 미결·잔여

- **클립보드 왕복은 하네스가 못 본다 (SKIP 1건).** Clipboard API 는 문서에 포커스가 없으면
  거부하고, Windows 는 백그라운드 프로세스를 대신해 창을 앞으로 내보내 주지 않는다
  (`setFocus()` 를 불러도 안 된다). **창을 클릭해 포커스를 준 상태로 하네스를 돌리면 그
  검사도 실제로 수행된다.** 지금은 조용히 통과시키지 않고 SKIP 으로 남긴다.
- **`reveal_path` 의 "선택된 상태로 열림"은 사람이 봐야 한다.** 하네스는 명령이 거부되지
  않는 것까지만 확인한다. 공백 있는 경로로 창이 뜨고 그 파일이 실제로 선택되는지 한 번 볼 것.
- 0.2.0 에는 업데이트 확인 코드가 없다(`v0.2.0` 태그에 `src/update.ts` 없음).
